# Fly.io Secrets Template
# Copy these values and set them with: fly secrets set KEY=value
#
# Generate secrets with:
#   openssl rand -base64 32  (for keys/salts)
#   openssl rand -base64 24  (for passwords)

# =============================================================================
# Required Secrets
# =============================================================================

# Database
POSTGRES_PASSWORD=

# Strapi CMS
CMS_APP_KEYS=key1,key2,key3,key4
CMS_API_TOKEN_SALT=
CMS_ADMIN_JWT_SECRET=
CMS_TRANSFER_TOKEN_SALT=
CMS_JWT_SECRET=

# API
API_JWT_SECRET=

# Strapi API Token (set after first deploy and creating token in CMS admin)
STRAPI_API_TOKEN=

# Public URLs (your fly.io app URL or custom domain)
NEXT_PUBLIC_STRAPI_URL=https://your-app-name.fly.dev
NEXT_PUBLIC_API_URL=https://your-app-name.fly.dev/api

# =============================================================================
# WhatsApp (Baileys)
# =============================================================================
# No secrets needed -- Baileys auth state is persisted to /data/baileys-auth
# on the Fly volume. Scan the QR code from worker logs on first deploy.
